streamlit>=1.30
pandas>=2.0
numpy>=1.24
openpyxl>=3.1
# Turso (hosted DB) is reached over HTTP using only the Python standard
# library — no extra package needed, so nothing can fail to build on a host.
